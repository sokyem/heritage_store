/** Flat surcharge added to a line when the shopper personalises a product
 *  (e.g. name + number on a jersey). Kept in one place so the storefront UI,
 *  the cart summary, and any server-side price checks all agree. */
export const CUSTOMIZATION_FEE = 15;
